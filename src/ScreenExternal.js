import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ScreenExternal = () => {
  return (
    <View>
      <Text>ScreenExternal</Text>
    </View>
  );
};

export default ScreenExternal;

const styles = StyleSheet.create({});
